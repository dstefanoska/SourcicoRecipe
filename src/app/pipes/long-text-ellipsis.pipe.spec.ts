import { LongTextEllipsisPipe } from './long-text-ellipsis.pipe';

describe('LongTextEllipsisPipe', () => {
  it('create an instance', () => {
    const pipe = new LongTextEllipsisPipe();
    expect(pipe).toBeTruthy();
  });
});
