import { TransformIngrediencePipe } from './transform-ingredience.pipe';

describe('TransformIngrediencePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformIngrediencePipe();
    expect(pipe).toBeTruthy();
  });
});
